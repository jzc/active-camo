{
    "private": true,
    "scripts": {
        "build": "webpack --config-name prod",
        "server": "webpack serve --config-name dev --open",
        "publish": "npm run build && gh-pages -d dist"
    },
    "dependencies": {
        "dat.gui": "^0.7.7",
        "three": "^0.128.0"
    },
    "devDependencies": {
        "gh-pages": "^3.1.0",
        "webpack": "^5.37.0",
        "webpack-cli": "^4.7.0",
        "webpack-dev-server": "^3.11.2",
        "webpack-merge": "^5.7.3"
    }
}
